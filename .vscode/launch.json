{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Flet调试(进入src)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/main.py",
            "cwd": "${workspaceFolder}/src", 
            "console": "integratedTerminal",
            "justMyCode": true
        }
    ]
}