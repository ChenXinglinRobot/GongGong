name: Build Android APK (Explicit Path)

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 1. 🔍 深度验尸：不仅看文件，还要看 pyproject.toml 有没有捣乱
      - name: Debug Environment
        run: |
          echo "=== 📂 文件列表 (ls -R) ==="
          ls -R
          echo "=== 📄 检查 main.py 是否存在 ==="
          if [ -f "main.py" ]; then echo "✅ main.py 存在于根目录"; else echo "❌ main.py 丢失!"; fi
          echo "=== ⚙️ 检查 pyproject.toml 配置 ==="
          cat pyproject.toml || echo "没有 pyproject.toml"

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      # 2. ✅ 保持这个协议修复，它工作得很好！
      - name: Accept Android Licenses
        run: |
          yes | flutter doctor --android-licenses

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: false

      - name: Install dependencies
        run: uv sync --reinstall

      - name: Clean Build
        run: rm -rf build/ dist/

      # 3. 📦 打包命令 (重大修改)
      # 变化1: --project "Gonggong" (去掉了空格)
      # 变化2: --module-name main (强制指定入口)
      # 变化3: 最后加了个点 "." (明确指定当前目录)
      - name: Build APK
        run: |
          uv run flet build apk --verbose --project "Gonggong" --module-name main .
        
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release-final
          path: "**/*.apk"