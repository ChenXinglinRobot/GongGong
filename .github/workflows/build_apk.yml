name: Build Android APK (Official Doc Verified)

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 1. 🔍 调试：让我看看你的 main.py 到底在哪
      - name: Debug File System
        run: |
          echo "=== 根目录文件列表 ==="
          ls -F
          if [ -f "main.py" ]; then
            echo "✅ 成功检测到 main.py，位置正确！"
          else
            echo "❌ 根目录没找到 main.py！请检查是否还在 src 里？"
            # 如果文件在 src，下面这行可以救命，但最好还是把文件搬出来
            # ls -F src/ 
          fi

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      # 2. ✅ 环境修复：这是文档里没写但必须做的！
      - name: Accept Android Licenses
        run: |
          yes | flutter doctor --android-licenses

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: false

      - name: Install dependencies
        run: uv sync --reinstall

      # 3. 🧹 清理缓存
      - name: Clean Build
        run: rm -rf build/ dist/

      # 4. 📦 打包：严格遵循官方文档默认行为
      # 默认路径就是 "." (当前目录)，所以不需要加额外参数
      - name: Build APK
        run: |
          uv run flet build apk --verbose --project "Gonggong App"
        
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release-verified
          path: "**/*.apk"